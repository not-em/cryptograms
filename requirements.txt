# Install the package with all local extras for development.
# Runtime dependencies are declared in pyproject.toml [project.dependencies].
# Web server dependencies are in [project.optional-dependencies.web].
-e .[dev,web]
